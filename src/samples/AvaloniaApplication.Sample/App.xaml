<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="clr-namespace:Avalonia.Xaml.Interactivity;assembly=Avalonia.Xaml.Interactivity"
             xmlns:ia="clr-namespace:Avalonia.Xaml.Interactions.Core;assembly=Avalonia.Xaml.Interactions"
					   xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:controls="clr-namespace:Xamarin.Forms.Platform.Avalonia.Controls;assembly=Xamarin.Forms.Platform.Avalonia"
             xmlns:converters="clr-namespace:Xamarin.Forms.Platform.Avalonia.Converters;assembly=Xamarin.Forms.Platform.Avalonia"
             x:Class="AvaloniaApplication.Sample.App">
  <Application.Styles>
    <StyleInclude Source="avares://Avalonia.Themes.Default/DefaultTheme.xaml"/>
    <StyleInclude Source="avares://Avalonia.Themes.Default/Accents/BaseLight.xaml"/>

    <Style Selector="controls|FormsTabbedPage">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="BarBackgroundColor" Value="{DynamicResource DefaultTabbedBarBackgroundColor}"/>
      <Setter Property="BarTextColor" Value="{DynamicResource DefaultTabbedBarTextColor}"/>
      <Setter Property="TitleBarBackgroundColor" Value="{DynamicResource DefaultTitleBarBackgroundColor}"/>
      <Setter Property="TitleBarTextColor" Value="{DynamicResource DefaultTitleBarTextColor}"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container" RowDefinitions="Auto,*">

              <TabControl Name="PART_TabControl" Items="{TemplateBinding ItemsSource}" SelectedItem="{TemplateBinding SelectedItem, Mode=TwoWay}" Height="40">
                <TabControl.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Title}" FontSize="18"></TextBlock>
                  </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                  <DataTemplate>
                    <Grid>
                    </Grid>
                  </DataTemplate>
                </TabControl.ContentTemplate>
              </TabControl>

              <controls:FormsTransitioningContentControl Grid.Row="1" Name="PART_Multi_Content"
                Transition="Left"
                ContentLoader="{TemplateBinding ContentLoader}"
                Source="{TemplateBinding SelectedItem}" />

            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsCarouselPage">
      <Setter Property="TitleBarBackgroundColor" Value="{DynamicResource DefaultTitleBarBackgroundColor}"/>
      <Setter Property="TitleBarTextColor" Value="{DynamicResource DefaultTitleBarTextColor}"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container">

              <controls:FormsTransitioningContentControl Name="PART_Multi_Content"
                        Transition="Left"
                        ContentLoader="{TemplateBinding ContentLoader}"
                        Source="{TemplateBinding SelectedItem}" />

              <Grid VerticalAlignment="Center" Name="PART_ButtonPanel">
                <RepeatButton Name="PART_NextButton"
                              FontFamily="Segoe UI Symbol"
                              Content=""
                              FontSize="18"
                              HorizontalAlignment="Right" />
                <RepeatButton Name="PART_PreviousButton"
                              FontFamily="Segoe UI Symbol"
                              Content=""
                              FontSize="18"
                              HorizontalAlignment="Left" />
              </Grid>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsNavigationPage">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="TitleBarBackgroundColor" Value="{DynamicResource DefaultTitleBarBackgroundColor}"/>
      <Setter Property="TitleBarTextColor" Value="{DynamicResource DefaultTitleBarTextColor}"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container">

              <controls:FormsTransitioningContentControl Name="PART_Navigation_Content" Transition="Normal"
                ContentLoader="{TemplateBinding ContentLoader}" Source="{TemplateBinding CurrentPage}" />

            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsMasterDetailPage">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="TitleBarBackgroundColor" Value="{DynamicResource DefaultTitleBarBackgroundColor}"/>
      <Setter Property="TitleBarTextColor" Value="{DynamicResource DefaultTitleBarTextColor}"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Border HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                  VerticalAlignment="{TemplateBinding VerticalAlignment}"
                  Background="{TemplateBinding Background}" >

            <Grid ColumnDefinitions="300,8,*">
              <Grid x:Name="PART_GridMaster" Grid.Column="0">
                <controls:FormsContentControl Grid.Column="0" Name="PART_Master"
                          ContentLoader="{TemplateBinding ContentLoader}" Source="{TemplateBinding MasterPage}" />
              </Grid>
              <GridSplitter x:Name="PART_GridSplitter" Width="8" Grid.Column="1" />
              <Grid x:Name="PART_GridDetailContent" Grid.Column="2">
                <controls:FormsContentControl Grid.Column="1" Name="PART_Detail_Content"
                          ContentLoader="{TemplateBinding ContentLoader}" Source="{TemplateBinding DetailPage}" />
              </Grid>
            </Grid>

          </Border>
        </ControlTemplate>
      </Setter>
    </Style>


    <Style Selector="controls|FormsSymbolIcon">
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="FontSize" Value="22"/>
      <Setter Property="Template">
        <ControlTemplate>
          <TextBlock Text="{Binding Symbol, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource SymbolToValue}}"
                     FontSize="{TemplateBinding FontSize}"
                     FontFamily="Segoe MDL2 Assets"/>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsPathIcon">
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="Height" Value="24"/>
      <Setter Property="Width" Value="24"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Viewbox>
            <Path Data="{Binding Data, RelativeSource={RelativeSource Mode=TemplatedParent}}"
              Fill="{TemplateBinding Foreground}"
              Stretch="Fill" />
          </Viewbox>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsBitmapIcon">
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="Height" Value="22.4"/>
      <Setter Property="Width" Value="22.4"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Image Source="{Binding UriSource, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsAppBar">
      <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
      <Setter Property="VerticalContentAlignment" Value="Stretch"/>
      <Setter Property="Padding" Value="0" />
      <Setter Property="Height" Value="32" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid  VerticalAlignment="Stretch" Background="{TemplateBinding Background}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ContentPresenter Grid.ColumnSpan="3"
                            Name="PART_ContentPresenter"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Content="{TemplateBinding Content}"
                            Padding="{TemplateBinding Padding}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"/>

            <ItemsControl Name="PART_Command" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Items="{TemplateBinding PrimaryCommands}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal"  Focusable="False" UseLayoutRounding="True"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
            </ItemsControl>

            <ToggleButton Name="PART_More" Grid.Column="2" Classes="AppBar" IsVisible="{Binding HasTopAppBar}">
              <controls:FormsSymbolIcon Symbol="More" FontSize="16" />
            </ToggleButton>
          </Grid>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsAppBarButton" >
      <Setter Property="Width"
              Value="72" />
      <Setter Property="Padding"
              Value="8, 5" />
      <Setter Property="Background"
              Value="{DynamicResource TransparentWhiteBrush}" />
      <Setter Property="HorizontalContentAlignment"
              Value="Center" />
      <Setter Property="VerticalContentAlignment"
              Value="Center" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid Background="{TemplateBinding Background}">
            <StackPanel VerticalAlignment="Center" >
              <ContentPresenter Content="{TemplateBinding Icon}" HorizontalAlignment="Center" />
              <TextBlock Margin="0,6,0,0" FontFamily="Calibri"  FontSize="12"
                     Text="{TemplateBinding Label}" VerticalAlignment="Bottom" HorizontalAlignment="Center" />
            </StackPanel>
          </Grid>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsWindowButtonCommands">
      <Setter Property="VerticalAlignment"
              Value="Center" />
      <Setter Property="Template">
        <ControlTemplate>
          <StackPanel Orientation="Horizontal">
            <Button x:Name="PART_Min"
                    Focusable="False"
                    IsEnabled="True">
              <Path Data="F1M0,6L0,9 9,9 9,6 0,6z"
                    Fill="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
            </Button>
            <Button x:Name="PART_Max"
                    Focusable="False"
                    IsEnabled="True">
              <!-- normal state -->
              <Path x:Name="PART_MaxPath"
                    Width="10"
                    Height="10"
                    Data="F1M0,0L0,9 9,9 9,0 0,0 0,3 8,3 8,8 1,8 1,3z"
                    Fill="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
            </Button>
            <Button x:Name="PART_Close"
                    Focusable="False">
              <Path Data="M 10.009,1.704 L 8.331,0.026 5.03,3.327 1.703,0 0,1.704 3.326,5.031 0.025,8.332 1.703,10.009 5.004,6.708 8.305,10.009 10.009,8.305 6.708,5.005"
                    Fill="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
            </Button>
          </StackPanel>
        </ControlTemplate>
      </Setter>
    </Style>


    <Style Selector="controls|FormsCollectionView">
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container">

              <controls:FormsTransitioningContentControl Name="PART_Multi_Content"
                        Transition="Left"
                        ContentLoader="{TemplateBinding ContentLoader}"
                        Source="{TemplateBinding SelectedItem}" />

            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsSwipeView">
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container">

              <controls:FormsTransitioningContentControl Name="PART_Multi_Content"
                        Transition="Left"
                        ContentLoader="{TemplateBinding ContentLoader}"
                        Source="{TemplateBinding SelectedItem}" />

            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsCarouselView">
      <Setter Property="Template">
        <ControlTemplate>
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Name="PART_Container">

              <controls:FormsTransitioningContentControl Name="PART_Multi_Content"
                        Transition="Left"
                        ContentLoader="{TemplateBinding ContentLoader}"
                        Source="{TemplateBinding SelectedItem}" />

              <Grid VerticalAlignment="Center" Name="PART_ButtonPanel">
                <RepeatButton Name="PART_NextButton"
                              FontFamily="Segoe UI Symbol"
                              Content=""
                              FontSize="18"
                              HorizontalAlignment="Right" />
                <RepeatButton Name="PART_PreviousButton"
                              FontFamily="Segoe UI Symbol"
                              Content=""
                              FontSize="18"
                              HorizontalAlignment="Left" />
              </Grid>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="controls|FormsContentDialog">
      <Setter Property="Width" Value="500" />
      <Setter Property="TitleTemplate" Value="{DynamicResource TitleDefaultTemplate}" />
      <Setter Property="Template">
        <ControlTemplate>
          <Border BorderBrush="{DynamicResource AccentColor}" Background="#FFF2F2F2" BorderThickness="2">
            <Grid Margin="24">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" MinHeight="150" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <!-- title -->
              <ContentControl Name="Title"
                          Content="{TemplateBinding Title}"
                          ContentTemplate="{TemplateBinding TitleTemplate}">
              </ContentControl>

              <!-- content -->
              <ContentPresenter Name="Content" Margin="0,20"
                      Content="{TemplateBinding Content}"
                      ContentTemplate="{TemplateBinding ContentTemplate}"
                      Grid.Row="1" >
              </ContentPresenter>

              <!-- buttons -->
              <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="PART_PrimaryButton" Content="{TemplateBinding PrimaryButtonText}"
                            Height="50"
                            MinWidth="140"
                            Margin="4,0,0,0"
                            BorderBrush="Transparent"
                          IsVisible="{TemplateBinding IsPrimaryButtonEnabled}" />
                <Button Name="PART_SecondaryButton" Content="{TemplateBinding SecondaryButtonText}"
                            Height="50"
                            MinWidth="140"
                            Margin="4,0,0,0"
                            BorderBrush="Transparent"
                          IsVisible="{TemplateBinding IsSecondaryButtonEnabled}" />
              </StackPanel>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>

    <Style Selector="Button.AppBar">
      <Setter Property="Background" Value="{DynamicResource CommandBarBackgroundColor}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarBackgroundColor}" />
      <Setter Property="Foreground" Value="{DynamicResource CommandBarTextColor}" />
    </Style>

    <Style Selector="Button.AppBar:pointerover">
      <Setter Property="Background" Value="{DynamicResource CommandBarBackgroundLightColor}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarBackgroundLightColor}" />
      <Setter Property="Foreground" Value="{DynamicResource CommandBarTextColor}" />
    </Style>

    <Style Selector="ToggleButton.AppBar">
      <Setter Property="Background" Value="{DynamicResource CommandBarBackgroundColor}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarBackgroundColor}" />
      <Setter Property="Foreground" Value="{DynamicResource CommandBarTextColor}" />
    </Style>

    <Style Selector="ToggleButton.AppBar:pointerover">
      <Setter Property="Background" Value="{DynamicResource CommandBarBackgroundLightColor}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarBackgroundLightColor}" />
      <Setter Property="Foreground" Value="{DynamicResource CommandBarTextColor}" />
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Stretch" />
      <Setter Property="Transition" Value="Default" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate>
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <Grid>
                <ContentPresenter Name="PreviousContentPresentationSite"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Content="{x:Null}"
                                Padding="{TemplateBinding Padding}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}">
                  <ContentPresenter.RenderTransform>
                    <TransformGroup>
                      <ScaleTransform />
                      <SkewTransform />
                      <RotateTransform />
                      <TranslateTransform />
                    </TransformGroup>
                  </ContentPresenter.RenderTransform>
                </ContentPresenter>

                <ContentPresenter Name="CurrentContentPresentationSite"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Content="{TemplateBinding Content}"
                                Padding="{TemplateBinding Padding}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}">
                  <ContentPresenter.RenderTransform>
                    <TransformGroup>
                      <ScaleTransform />
                      <SkewTransform />
                      <RotateTransform />
                      <TranslateTransform />
                    </TransformGroup>
                  </ContentPresenter.RenderTransform>
                </ContentPresenter>
              </Grid>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Default] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Default] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Normal] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Setter Property="IsVisible" Value="True"/>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Up] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="TranslateTransform.Y" Value="30"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
            <Setter Property="TranslateTransform.Y" Value="0"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Up] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
            <Setter Property="TranslateTransform.Y" Value="0"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="TranslateTransform.Y" Value="-30"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Down] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="TranslateTransform.Y" Value="-40"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
            <Setter Property="TranslateTransform.Y" Value="0"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Down] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
            <Setter Property="TranslateTransform.Y" Value="0"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="TranslateTransform.Y" Value="40"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Right] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
        </Animation>
        <Animation Duration="00:00:00.200" Easing="BounceEaseOut">
          <KeyFrame Cue="0%">
            <Setter Property="TranslateTransform.X" Value="-30"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
        </Animation>

      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Right] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
        </Animation>
        <Animation Duration="00:00:00.200" Easing="BounceEaseOut">
          <KeyFrame Cue="0%">
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="TranslateTransform.X" Value="30"/>
          </KeyFrame>
        </Animation>

      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Left] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
        </Animation>
        <Animation Duration="00:00:00.200" Easing="BounceEaseOut">
          <KeyFrame Cue="0%">
            <Setter Property="TranslateTransform.X" Value="30"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
        </Animation>

      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=Left] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
        </Animation>
        <Animation Duration="00:00:00.200" Easing="BounceEaseOut">
          <KeyFrame Cue="0%">
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="TranslateTransform.X" Value="-30"/>
          </KeyFrame>
        </Animation>

      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=RightReplace] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
        </Animation>
        <Animation Duration="00:00:00.200" Easing="BounceEaseOut">
          <KeyFrame Cue="0%">
            <Setter Property="TranslateTransform.X" Value="-40"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
        </Animation>

      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=RightReplace] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=LeftReplace] /template/ ContentPresenter#CurrentContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="TranslateTransform.X" Value="40"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="True"/>
            <Setter Property="TranslateTransform.X" Value="0"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>

    <Style Selector="controls|FormsTransitioningContentControl[Transition=LeftReplace] /template/ ContentPresenter#PreviousContentPresentationSite">
      <Style.Animations>
        <Animation Duration="00:00:00.200">
          <KeyFrame Cue="0%">
            <Setter Property="IsVisible" Value="True"/>
          </KeyFrame>
          <KeyFrame Cue="100%">
            <Setter Property="IsVisible" Value="False"/>
          </KeyFrame>
        </Animation>
      </Style.Animations>
    </Style>
  </Application.Styles>

  <Application.Resources>
    <!-- Default Global Color -->
    <SolidColorBrush x:Key="WindowBackgroundColor" Color="White" />
    <SolidColorBrush x:Key="AccentColor" Color="#3498db" />

    <!-- Default Command Bar Color -->
    <SolidColorBrush x:Key="CommandBarBackgroundColor" Color="#3498db" />
    <SolidColorBrush x:Key="CommandBarBackgroundLightColor" Color="#78bae7" />
    <SolidColorBrush x:Key="CommandBarTextColor" Color="White" />

    <!-- Default Title Bar Color -->
    <SolidColorBrush x:Key="DefaultTitleBarBackgroundColor" Color="#3498db" />
    <SolidColorBrush x:Key="DefaultTitleBarTextColor" Color="White" />

    <!-- Default Tabbed Bar Color -->
    <SolidColorBrush x:Key="DefaultTabbedBarBackgroundColor" Color="#3498db" />
    <SolidColorBrush x:Key="DefaultTabbedBarTextColor" Color="White" />

    <system:Double x:Key="FontSizeSmall">18</system:Double>
    <system:Double x:Key="FontSizeNormal">20</system:Double>
    <system:Double x:Key="FontSizeMedium">22</system:Double>
    <system:Double x:Key="FontSizeMediumLarge">25</system:Double>
    <system:Double x:Key="FontSizeLarge">32</system:Double>
    <system:Double x:Key="FontSizeExtraLarge">42</system:Double>
    <system:Double x:Key="FontSizeExtraExtraLarge">72</system:Double>
    <system:Double x:Key="FontSizeHuge">186</system:Double>
    <system:Double x:Key="FontSizeBody">14</system:Double>
    <system:Double x:Key="FontSizeCaption">12</system:Double>
    <system:Double x:Key="FontSizeHeader">46</system:Double>
    <system:Double x:Key="FontSizeSubtitle">20</system:Double>
    <system:Double x:Key="FontSizeTitle">24</system:Double>

    <FontFamily x:Key="FontFamilyNormal">Segoe UI</FontFamily>
    <FontFamily x:Key="FontFamilySemiBold">Segoe UI</FontFamily>

    <converters:HeightConverter x:Key="HeightConverter"/>
    <converters:ViewToRendererConverter x:Key="ViewToRenderer"/>
    <converters:CaseConverter x:Key="LowerConverter" ConvertToUpper="False" />
    <converters:CaseConverter x:Key="UpperConverter" ConvertToUpper="True" />
    <converters:CollapseWhenEmptyConverter x:Key="CollapseWhenEmpty" />
    <converters:ColorConverter x:Key="ColorConverter" />
    <converters:ImageConverter x:Key="ImageConverter" />
    <converters:SymbolToValueConverter x:Key="SymbolToValue" />


    <DataTemplate x:Key="CellTemplate">
      <controls:CellControl Cell="{Binding}" Margin="5,0,0,0"
               Height="{Binding Cell.RenderHeight,RelativeSource={RelativeSource Mode=Self}, Converter={StaticResource HeightConverter}}" >
      </controls:CellControl>
    </DataTemplate>

    <DataTemplate x:Key="TableSectionHeader">
      <StackPanel>
        <TextBlock Text="{Binding Title, Converter={StaticResource LowerConverter}}" FontSize="24">
          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="TextColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>
        <TextBlock
           Text="{Binding Text}"
           IsVisible="{Binding Text,RelativeSource={RelativeSource Self}, Converter={StaticResource CollapseWhenEmpty}}">
          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="TextColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="TextCell">
      <StackPanel>
        <TextBlock
          Text="{Binding Text}"
          IsVisible="{Binding Text,RelativeSource={RelativeSource Self}, Converter={StaticResource CollapseWhenEmpty}}">

          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="TextColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>

        <TextBlock
          Text="{Binding Detail}"
          IsVisible="{Binding Text,RelativeSource={RelativeSource Self}, Converter={StaticResource CollapseWhenEmpty}}">
          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="DetailColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="SwitchCell">
      <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Text}" VerticalAlignment="Center" />
        <CheckBox Grid.Column="1" IsChecked="{Binding On, Mode=TwoWay}" VerticalAlignment="Center" />
      </Grid>
    </DataTemplate>

    <DataTemplate x:Key="ImageCell">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
        </Grid.RowDefinitions>

        <Image Grid.Column="0" Grid.RowSpan="2"
          DataContext="{Binding ImageSource, Converter={StaticResource ImageConverter}}" Source="{Binding Value}"
          VerticalAlignment="Center" />

        <TextBlock Grid.Column="1" Grid.Row="0"
          Text="{Binding Text}"
          IsVisible="{Binding Text,RelativeSource={RelativeSource Self}, Converter={StaticResource CollapseWhenEmpty}}">
          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="TextColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>

        <TextBlock Grid.Column="1" Grid.Row="1"
          Text="{Binding Detail}"
          IsVisible="{Binding Text,RelativeSource={RelativeSource Self}, Converter={StaticResource CollapseWhenEmpty}}">
          <TextBlock.Foreground>
            <MultiBinding Converter="{StaticResource ColorConverter}" ConverterParameter="{x:Static TextBlock.ForegroundProperty}">
              <Binding RelativeSource="{RelativeSource Self}"/>
              <Binding Path="DetailColor"/>
            </MultiBinding>
          </TextBlock.Foreground>
        </TextBlock>
      </Grid>
    </DataTemplate>

    <DataTemplate x:Key="EntryCell">

      <controls:FormsTextBox Grid.Column="1" Text="{Binding Text, Mode=TwoWay}" VerticalAlignment="Center" IsEnabled="{Binding IsEnabled}"/>

    </DataTemplate>

    <DataTemplate x:Key="ViewCell">
      <ContentPresenter Content="{Binding View, Converter={StaticResource ViewToRenderer}}" />
    </DataTemplate>

    <DataTemplate x:Key="TitleDefaultTemplate" >
      <TextBlock DataContext="{Binding}"
                 Text="{Binding}"
                 FontFamily="Segoe UI"
                 FontSize="26"
                 Foreground="Black"/>
    </DataTemplate>

    <Style x:Key="BaseLightWindowButtonStyle" Selector="Button">
      <Setter Property="Foreground"
              Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type Control}}, Path=(TextElement.Foreground)}" />
      <Setter Property="Background"
              Value="{DynamicResource TransparentWhiteBrush}" />
      <Setter Property="HorizontalContentAlignment"
              Value="Center" />
      <Setter Property="VerticalContentAlignment"
              Value="Center" />
      <Setter Property="Padding"
              Value="1" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid Background="{TemplateBinding Background}">
            <ContentPresenter Name="contentPresenter"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="{TemplateBinding Padding}"
                              Opacity="1.0" />
          </Grid>
        </ControlTemplate>
      </Setter>
      <i:Interaction.Behaviors>
        <ia:DataTriggerBehavior Binding="IsPointerOver" Value="True">
          <ia:ChangePropertyAction TargetObject="{Binding #contentPresenter}" PropertyName="Background" Value="{DynamicResource SemiTransparentWhiteBrush}"/>
        </ia:DataTriggerBehavior>
      </i:Interaction.Behaviors>
    </Style>


    <Style x:Key="LightOtherWindowButtonStyle" Selector="Button">
      <Setter Property="Padding"
              Value="14, 5" />
    </Style>

    <Style x:Key="LightCommandWindowButtonStyle" Selector="Button">
      <Setter Property="Width"
              Value="45" />
      <Setter Property="Padding"
              Value="14, 5" />
    </Style>



    <Style x:Key="LightRedCommandWindowButtonStyle" Selector="Button">
      <i:Interaction.Behaviors>
        <ia:DataTriggerBehavior Binding="IsPointerOver" Value="True">
          <ia:ChangePropertyAction PropertyName="Background" Value="Red"/>
        </ia:DataTriggerBehavior>
      </i:Interaction.Behaviors>
    </Style>

    <Style x:Key="AppBarButtonStyle" Selector="controls|FormsAppBar">
      <Setter Property="Foreground"
              Value="White" />
      <Setter Property="Width"
              Value="72" />
      <Setter Property="Padding"
              Value="8, 5" />
    </Style>


    <Style x:Key="BaseLightWindowToggleButtonStyle" Selector="Button">
      <Setter Property="Foreground"
              Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type Control}}, Path=(TextElement.Foreground)}" />
      <Setter Property="Background"
              Value="{DynamicResource TransparentWhiteBrush}" />
      <Setter Property="HorizontalContentAlignment"
              Value="Center" />
      <Setter Property="VerticalContentAlignment"
              Value="Center" />
      <Setter Property="Padding"
              Value="1" />
      <Setter Property="Template">
        <ControlTemplate>
          <Grid Background="{TemplateBinding Background}">
            <ContentPresenter Name="contentPresenter"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="{TemplateBinding Padding}"
                              Opacity="1.0" />
          </Grid>
        </ControlTemplate>
      </Setter>
      <i:Interaction.Behaviors>
        <ia:DataTriggerBehavior Binding="IsPointerOver" Value="True">
          <ia:ChangePropertyAction PropertyName="Background" Value="{DynamicResource SemiTransparentWhiteBrush}"/>
        </ia:DataTriggerBehavior>
      </i:Interaction.Behaviors>
    </Style>

    <Style x:Key="AppBarToggleButtonStyle" Selector="controls|FormsAppBarButton">
      <Setter Property="Width"
              Value="72" />
      <Setter Property="Padding"
              Value="8, 5" />
    </Style>

  </Application.Resources>
</Application>
